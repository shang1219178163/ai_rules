---
name: api
description: REST API 设计规范。设计或修改 HTTP API 时使用。
globs: "**/*.{yaml,yml,json}"
alwaysApply: false
paths:
  - "**/*api*/**"
  - "**/*controller*"
  - "**/*handler*"
  - "**/*.yaml"
  - "**/*.yml"
---
# API
RESTful；HTTP 状态码规范；统一返回结构；分页、排序、过滤；幂等；API 版本；URI 使用名词。
